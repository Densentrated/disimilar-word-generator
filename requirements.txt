# Python Version Compatibility:
# - Python 3.12 and earlier: Install all dependencies below
# - Python 3.13+: Skip wikiextractor and use extract_text_simple.py instead

# Core dependencies
numpy>=1.21.0
pandas>=1.3.0
fasttext>=0.9.2
tqdm>=4.62.0

# Vietnamese NLP
underthesea>=1.3.0
pyvi>=0.1.1

# Wikipedia extraction (Python 3.12 and earlier only)
# WARNING: wikiextractor has regex compatibility issues with Python 3.13+
# For Python 3.13+, use extract_text_simple.py instead
wikiextractor==3.0.4; python_version < '3.13'

# Additional utilities
requests>=2.26.0
